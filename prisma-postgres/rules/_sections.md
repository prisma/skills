# sections

Rule categories for `prisma-postgres`.

| Section | Prefix | Description |
|---|---|---|
| Console and Connections | `console-and-connections` | Console, Studio, and connection workflows |
| CLI Provisioning | `create-db-cli` | `create-db`/`create-pg`/`create-postgres` usage |
| Management API | `management-api` | Programmatic provisioning via REST API |
| Management API SDK | `management-api-sdk` | Typed TypeScript integration and OAuth flow |
