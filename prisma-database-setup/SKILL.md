---
name: prisma-database-setup
description: Deprecated compatibility name for prisma-orm-setup. Use only when a user or existing integration explicitly invokes prisma-database-setup; otherwise select prisma-orm-setup.
license: MIT
metadata:
  author: prisma
  version: "8.0.0"
---

# Deprecated: prisma-database-setup

Load `prisma-orm-setup` with the host's skill loader or read its installed `SKILL.md`. If unavailable, install it from [prisma/skills](https://github.com/prisma/skills/tree/main/prisma-orm-setup) before continuing.

This compatibility entry contains no setup recipes. Keep it until consumers have migrated to the replacement.
